start
	var a: int;
	var b: int;
	var c: int;

	read(a);
	read(b);
	read(c);

	var max: int = a;

	if (max < b) then{
        max = b;
    };

    if (max <= c) then {
        max = c;
    };

	print("MaxIs");
	print(max);
finish